package advanced.designpatterns.factory;

/**
 * Acts as the Creator interface (Factory Interface) with a method
 * createVehicle() representing the factory method.
 */

public interface VehicleFactory {
    Vehicle createVehicle();
}
