package intermediate.interfaces;

public interface SuperSayiajinPowers {
    /* Interfaces
     * 1. Cannot create objects
     * 2. Cannot add body to their methods
     * 3. Every time you add an attribute it must have value, because its FINAL by default
     * 4. Every single method is ABSTRACT by default, so when you implement an interface, its mandatory to initialize its methods*/

    void genkiDama();
    void kamehameha();
}
